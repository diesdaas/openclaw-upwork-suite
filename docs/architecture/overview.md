# Architecture Overview

This directory contains the high-level architecture designs, agent boundaries, and module definitions for the `openclaw-upwork-suite`.

The core architectural goal is to migrate 4 independent repositories into a single monorepo while preserving isolation (minimal disclosure, distinct agent roles) using explicit, strongly-typed contracts.
