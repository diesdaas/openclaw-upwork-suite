# Development Runbook

*This is a placeholder runbook for local development instructions.*

Once the migration is complete, this document will describe how to:
1. Install dependencies (`pnpm install`).
2. Run the full suite using Turborepo (`turbo run dev`).
3. Handle database migrations for the central DB.
4. Set up local `.env` files for the orchestrator and agents.
